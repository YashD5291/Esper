---
phase: 05-telegram-hardening
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_telegram_sender.py
  - src/telegram_sender.py
  - src/config.py
  - tests/test_config.py
  - src/server.py
autonomous: true
requirements: [INTG-01, INTG-02]

must_haves:
  truths:
    - "Two sequential utterances both produce two separate Telegram sendMessage calls"
    - "Stopping the pipeline drains queued messages within a 10-second window"
    - "A 429 rate-limit response sleeps for the server-specified retry_after duration"
    - "Messages that fail after max retries are logged at ERROR and dropped — pipeline continues"
    - "No _processed_chars, _draft_buf, _draft_id, _last_draft_t, _prev_sentence_count, or stream attributes exist on TelegramSender"
  artifacts:
    - path: "tests/test_telegram_sender.py"
      provides: "Unit tests for per-utterance sender, 429 handling, shutdown drain"
      min_lines: 80
    - path: "src/telegram_sender.py"
      provides: "Simplified per-utterance TelegramSender"
      min_lines: 40
    - path: "src/config.py"
      provides: "Telegram config without dead TELEGRAM_STREAM and TELEGRAM_DRAFT_INTERVAL"
    - path: "src/server.py"
      provides: "Updated _do_stop with 10s timeout, constructor without stream="
  key_links:
    - from: "src/server.py"
      to: "src/telegram_sender.py"
      via: "TelegramSender(bot_token, chat_id) constructor call"
      pattern: "TelegramSender\\(bot_token, chat_id\\)"
    - from: "src/server.py"
      to: "src/telegram_sender.py"
      via: "_telegram_sender.wait(timeout=10.0) in _do_stop"
      pattern: "wait\\(timeout=10\\.0\\)"
    - from: "src/telegram_sender.py"
      to: "Telegram API"
      via: "_send_message with 429 retry_after parsing"
      pattern: "resp\\.status_code == 429"
---

<objective>
Rewrite TelegramSender for the per-utterance TranscriptionUpdate contract and harden shutdown/retry behavior.

Purpose: The current TelegramSender accumulates text across utterances via `_processed_chars` and supports draft streaming — both models are broken under the Phase 4 per-utterance contract. This plan strips all cross-utterance state, implements 429 handling, and increases the shutdown drain window to 10 seconds.

Output: A simplified, tested TelegramSender that sends one message per utterance, handles rate limits, and flushes reliably on stop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-telegram-hardening/05-CONTEXT.md
@.planning/phases/05-telegram-hardening/05-RESEARCH.md

@src/telegram_sender.py
@src/config.py
@src/server.py
@tests/test_config.py

<interfaces>
<!-- Phase 4 TranscriptionUpdate contract (from 04-CONTEXT.md D-01) -->
<!-- Phase 5 tests must mock this shape since Phase 4 may not be implemented yet -->

Expected Phase 4 TranscriptionUpdate shape:
```python
@dataclass
class TranscriptionUpdate:
    text: str               # This utterance's transcription
    finalized_text: str     # Session accumulator
    sentences: list         # All finalized sentences
    no_speech_prob: float   # Whisper confidence
    duration_s: float       # Utterance duration
```

Phase 5 tests should define a local stub:
```python
from dataclasses import dataclass

@dataclass
class TranscriptionUpdate:
    text: str
```

Current TelegramSender constructor (server.py line 207):
```python
_telegram_sender = TelegramSender(bot_token, chat_id, stream=stream)
```

Current _do_stop timeout (server.py line 283):
```python
_telegram_sender.wait(timeout=5.0)
```

Dead config constants to remove (config.py lines 31, 34):
```python
TELEGRAM_STREAM: bool = True
TELEGRAM_DRAFT_INTERVAL: float = 0.5
```

Dead test functions to remove (test_config.py lines 109-111, 124-126):
```python
def test_telegram_stream_default_true(): ...
def test_telegram_draft_interval(): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — Write tests then rewrite TelegramSender for per-utterance model</name>
  <files>tests/test_telegram_sender.py, src/telegram_sender.py</files>
  <read_first>
    - src/telegram_sender.py (current implementation to understand what to strip)
    - src/config.py (TELEGRAM_MAX_RETRIES, TELEGRAM_BACKOFF_BASE values used in tests)
    - .planning/phases/05-telegram-hardening/05-RESEARCH.md (Pattern 1-3, Pitfalls 1-4, code examples)
    - .planning/phases/05-telegram-hardening/05-CONTEXT.md (locked decisions D-01 through D-07)
    - tests/test_vad.py (test style reference — project uses pytest, no classes, minimal fixtures)
  </read_first>
  <behavior>
    RED phase — create tests/test_telegram_sender.py with these test cases:

    1. test_two_utterances_send_two_messages (INTG-01):
       - Create TelegramSender("tok", "123")
       - Mock httpx.Client.post to return status_code=200
       - Call on_update(TranscriptionUpdate(text="Hello")) then on_update(TranscriptionUpdate(text="World"))
       - Call stop() + wait(timeout=5)
       - Assert httpx.Client.post was called exactly 2 times
       - Assert call args contain text="Hello" and text="World" respectively

    2. test_empty_utterance_not_sent (INTG-01):
       - Create TelegramSender("tok", "123")
       - Call on_update(TranscriptionUpdate(text="")) and on_update(TranscriptionUpdate(text="   "))
       - Call stop() + wait(timeout=5)
       - Assert httpx.Client.post was NOT called

    3. test_dead_state_removed (INTG-01, D-02, D-03):
       - Create TelegramSender("tok", "123")
       - Assert not hasattr(sender, "_processed_chars")
       - Assert not hasattr(sender, "_draft_buf")
       - Assert not hasattr(sender, "_draft_id")
       - Assert not hasattr(sender, "_last_draft_t")
       - Assert not hasattr(sender, "_prev_sentence_count")
       - Assert not hasattr(sender, "_stream")
       - Call stop() + wait(timeout=5)

    4. test_shutdown_drains_queue (INTG-02, D-04, D-05):
       - Create TelegramSender("tok", "123")
       - Mock httpx.Client.post with a 0.5s sleep to simulate latency, then return 200
       - Enqueue 3 messages via on_update()
       - Call stop()
       - Call wait(timeout=10.0) — assert it returns (thread joins within 10s)
       - Assert all 3 messages were sent (post called 3 times)

    5. test_429_respects_retry_after (D-06):
       - Create TelegramSender("tok", "123")
       - Mock httpx.Client.post: first call returns 429 with JSON body {"ok": false, "error_code": 429, "parameters": {"retry_after": 1}}, second call returns 200
       - Call on_update(TranscriptionUpdate(text="Hi"))
       - Call stop() + wait(timeout=15)
       - Assert post was called 2 times (retry succeeded)
       - Assert time.sleep was called with 1 (the retry_after value)

    6. test_max_retries_drops_message (D-07):
       - Create TelegramSender("tok", "123")
       - Mock httpx.Client.post to always return status_code=500
       - Call on_update(TranscriptionUpdate(text="fail"))
       - Call stop() + wait(timeout=10)
       - Assert post was called exactly config.TELEGRAM_MAX_RETRIES times (3)
       - Assert log.error was called with message containing "Failed to send"

    7. test_wait_default_timeout_is_10 (INTG-02, D-05):
       - Inspect TelegramSender.wait signature
       - Assert default timeout parameter is 10.0

    Use a local TranscriptionUpdate stub (dataclass with text field) — do NOT import from src.transcriber. This decouples from Phase 4.

    Mock httpx.Client using unittest.mock.patch on the httpx.Client class. The _loop() method creates its own client, so patch httpx.Client to return a mock whose .post() method is controllable.

    GREEN phase — rewrite src/telegram_sender.py:

    Module docstring: "Per-utterance Telegram sender for Esper."

    Remove all imports of TranscriptionUpdate — on_update now takes a duck-typed object with .text attribute.

    class TelegramSender:
        __init__(self, bot_token: str, chat_id: str) -> None:
            - self._base_url = f"https://api.telegram.org/bot{bot_token}"
            - self._chat_id = chat_id
            - self._queue: queue.Queue[str | None] = queue.Queue()
            - self._thread = threading.Thread(target=self._loop, daemon=True, name="telegram-sender")
            - self._thread.start()
            - NO _stream, _processed_chars, _draft_buf, _draft_id, _last_draft_t, _prev_sentence_count

        on_update(self, update) -> None:
            - text = update.text.strip()
            - if text: self._queue.put(text)
            - No type annotation for update — duck typing (accepts anything with .text)

        _loop(self) -> None:
            - client = httpx.Client(timeout=10.0)
            - try: while True: item = self._queue.get(); if item is None: break; self._send_message(client, item)
            - finally: client.close()
            - NO _process_streaming, NO _send_draft, NO final draft flush

        _send_message(self, client: httpx.Client, text: str) -> None:
            - for attempt in range(config.TELEGRAM_MAX_RETRIES):
            -   try: resp = client.post(f"{self._base_url}/sendMessage", json={"chat_id": self._chat_id, "text": text})
            -   if resp.status_code == 200: log.debug("Sent: %s", text[:40]); return
            -   if resp.status_code == 429:
            -     try: body = resp.json(); retry_after = body.get("parameters", {}).get("retry_after")
            -     except (ValueError, KeyError): retry_after = None
            -     wait_time = retry_after or config.TELEGRAM_BACKOFF_BASE * (2 ** attempt)
            -     log.warning("Telegram 429: retry after %ss (attempt %d)", wait_time, attempt + 1)
            -     time.sleep(wait_time); continue
            -   log.warning("Telegram API %d: %s", resp.status_code, resp.text[:200])
            -   except (httpx.TransportError, httpx.TimeoutException) as exc:
            -     log.warning("Telegram send error (attempt %d): %s", attempt + 1, exc)
            -   time.sleep(config.TELEGRAM_BACKOFF_BASE * (2 ** attempt))
            - log.error("Failed to send after %d attempts: %s", config.TELEGRAM_MAX_RETRIES, text[:60])

        stop(self) -> None:
            - self._queue.put(None)

        wait(self, timeout: float = 10.0) -> None:
            - self._thread.join(timeout=timeout)

    Remove: _process_streaming, _send_draft methods entirely.
    Remove: import of TranscriptionUpdate from .transcriber.
  </behavior>
  <action>
    RED: Create tests/test_telegram_sender.py with all 7 test cases described above. Define a local TranscriptionUpdate stub at the top of the test file:

    ```python
    from dataclasses import dataclass
    @dataclass
    class TranscriptionUpdate:
        text: str
    ```

    Use unittest.mock.patch("httpx.Client") to mock the HTTP client. The mock's return_value gives the mock instance whose .post() is controllable. Use unittest.mock.patch("time.sleep") to verify retry_after sleep durations without actually sleeping.

    Run: `python3 -m pytest tests/test_telegram_sender.py -x -q` — all tests MUST FAIL (RED).

    GREEN: Rewrite src/telegram_sender.py per the behavior spec above. Remove all dead state (per D-01, D-02, D-03). Add 429 handling (per D-06). Change wait() default to 10.0 (per D-05). Log and drop after max retries (per D-07).

    Run: `python3 -m pytest tests/test_telegram_sender.py -x -q` — all tests MUST PASS (GREEN).

    Commit RED: `test(05-01): add failing tests for per-utterance TelegramSender`
    Commit GREEN: `feat(05-01): rewrite TelegramSender for per-utterance model`
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_telegram_sender.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_telegram_sender.py contains `def test_two_utterances_send_two_messages`
    - tests/test_telegram_sender.py contains `def test_empty_utterance_not_sent`
    - tests/test_telegram_sender.py contains `def test_dead_state_removed`
    - tests/test_telegram_sender.py contains `def test_shutdown_drains_queue`
    - tests/test_telegram_sender.py contains `def test_429_respects_retry_after`
    - tests/test_telegram_sender.py contains `def test_max_retries_drops_message`
    - tests/test_telegram_sender.py contains `def test_wait_default_timeout_is_10`
    - src/telegram_sender.py does NOT contain `_processed_chars`
    - src/telegram_sender.py does NOT contain `_draft_buf`
    - src/telegram_sender.py does NOT contain `_send_draft`
    - src/telegram_sender.py does NOT contain `_process_streaming`
    - src/telegram_sender.py does NOT contain `_prev_sentence_count`
    - src/telegram_sender.py does NOT contain `stream`
    - src/telegram_sender.py contains `resp.status_code == 429`
    - src/telegram_sender.py contains `retry_after`
    - src/telegram_sender.py contains `timeout: float = 10.0`
    - `python3 -m pytest tests/test_telegram_sender.py -x -q` exits 0
  </acceptance_criteria>
  <done>All 7 tests pass. TelegramSender has no cross-utterance state. 429 responses parse retry_after from JSON body. wait() defaults to 10.0s. Messages dropped after max retries with ERROR log.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead config + update server.py constructor and timeout</name>
  <files>src/config.py, tests/test_config.py, src/server.py</files>
  <read_first>
    - src/config.py (lines 29-34 — Telegram section with dead constants)
    - tests/test_config.py (lines 109-126 — dead config tests to remove)
    - src/server.py (line 203-208 — TelegramSender constructor, line 281-284 — _do_stop timeout)
  </read_first>
  <action>
    1. Edit src/config.py:
       - Delete line `TELEGRAM_STREAM: bool = True`
       - Delete line `TELEGRAM_DRAFT_INTERVAL: float = 0.5  # min seconds between draft updates`
       - Keep all other Telegram constants (BOT_TOKEN, CHAT_ID, MAX_RETRIES, BACKOFF_BASE)

    2. Edit tests/test_config.py:
       - Delete the `test_telegram_stream_default_true` function (lines 109-111)
       - Delete the `test_telegram_draft_interval` function (lines 124-126)

    3. Edit src/server.py _do_start() (around line 198-208):
       - Remove: `stream = telegram_cfg.get("stream", True)`
       - Remove: `config.TELEGRAM_STREAM = stream`
       - Change: `_telegram_sender = TelegramSender(bot_token, chat_id, stream=stream)` to `_telegram_sender = TelegramSender(bot_token, chat_id)`
       - Change: `log.info("Telegram sender configured (stream=%s)", stream)` to `log.info("Telegram sender configured")`

    4. Edit src/server.py _do_stop() (around line 283):
       - Change: `_telegram_sender.wait(timeout=5.0)` to `_telegram_sender.wait(timeout=10.0)`

    Run full test suite to verify nothing breaks.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/config.py does NOT contain `TELEGRAM_STREAM`
    - src/config.py does NOT contain `TELEGRAM_DRAFT_INTERVAL`
    - src/config.py contains `TELEGRAM_MAX_RETRIES: int = 3`
    - src/config.py contains `TELEGRAM_BACKOFF_BASE: float = 1.0`
    - tests/test_config.py does NOT contain `test_telegram_stream_default_true`
    - tests/test_config.py does NOT contain `test_telegram_draft_interval`
    - src/server.py contains `TelegramSender(bot_token, chat_id)` (no stream= kwarg)
    - src/server.py does NOT contain `stream=stream`
    - src/server.py does NOT contain `config.TELEGRAM_STREAM`
    - src/server.py contains `wait(timeout=10.0)` in _do_stop
    - src/server.py does NOT contain `wait(timeout=5.0)` in the telegram shutdown section
    - `python3 -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>Dead config constants removed. Dead config tests removed. server.py constructs TelegramSender without stream= param. _do_stop() uses 10.0s timeout. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_telegram_sender.py -x -q` — all 7 per-utterance tests pass
2. `python3 -m pytest tests/test_config.py -x -q` — config tests pass without dead constant tests
3. `python3 -m pytest tests/ -x -q` — full suite green
4. `grep -c "_processed_chars\|_draft_buf\|_send_draft\|_process_streaming\|_prev_sentence_count" src/telegram_sender.py` — returns 0
5. `grep -c "TELEGRAM_STREAM\|TELEGRAM_DRAFT_INTERVAL" src/config.py` — returns 0
6. `grep "wait(timeout=10.0)" src/server.py` — returns match in _do_stop
</verification>

<success_criteria>
- TelegramSender sends exactly one message per utterance with no cross-utterance state (INTG-01)
- Shutdown flushes all queued messages within 10 seconds (INTG-02)
- 429 responses parse retry_after from Telegram JSON and sleep that duration (D-06)
- Messages dropped after max retries with ERROR log (D-07)
- All dead streaming/draft code and config removed (D-01, D-02, D-03)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-telegram-hardening/05-01-SUMMARY.md`
</output>
