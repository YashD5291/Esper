---
phase: 06-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/coreml_transcriber.py      # DELETE
  - src/transcriber.py             # Remove backward-compat fields
  - src/config.py                  # Remove DEFAULT_ENGINE
  - src/server.py                  # Remove engine selection remnants
  - requirements.txt               # Remove parakeet-mlx, coremltools, scipy
  - models/coreml/                 # DELETE (git rm -r)
  - tests/test_stt.py              # DELETE
  - tests/test_config.py           # Remove test_default_engine
  - tests/test_whisper_transcriber.py  # Remove backward-compat assertions
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "No Python file in src/ imports coreml_transcriber"
    - "No Python file in src/ references DEFAULT_ENGINE"
    - "TranscriptionUpdate has no backward-compat fields (draft_text, finalized_sentences, draft_sentences)"
    - "requirements.txt has no parakeet-mlx, coremltools, or scipy entries"
    - "models/coreml/ directory does not exist"
    - "pip install -r requirements.txt succeeds cleanly"
    - "All existing tests pass"
  artifacts:
    - path: "src/config.py"
      provides: "Clean config with no DEFAULT_ENGINE constant"
      contains: "MODEL_LOAD_TIMEOUT_S"
    - path: "src/transcriber.py"
      provides: "WhisperTranscriber + clean TranscriptionUpdate"
      exports: ["TranscriptionUpdate", "WhisperTranscriber"]
    - path: "requirements.txt"
      provides: "Clean dependency list"
  key_links:
    - from: "src/server.py"
      to: "src/transcriber.py"
      via: "from .transcriber import TranscriptionUpdate, WhisperTranscriber"
      pattern: "from \\.transcriber import"
    - from: "src/realtime_demo.py"
      to: "src/transcriber.py"
      via: "from .transcriber import TranscriptionUpdate, WhisperTranscriber"
      pattern: "from \\.transcriber import"
    - from: "src/telegram_sender.py"
      to: "src/transcriber.py"
      via: "from .transcriber import TranscriptionUpdate"
      pattern: "from \\.transcriber import TranscriptionUpdate"
---

<objective>
Remove all Parakeet/CoreML dead code, model artifacts, and stale dependencies from the codebase. After this plan, only the v2.0 Whisper-based architecture remains.

Purpose: CLEAN-01 (delete Parakeet transcriber code) and CLEAN-02 (remove dead dependencies). The dual-engine abstraction is eliminated.
Output: A clean codebase with no references to CoreML, Parakeet, or engine selection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanup/06-CONTEXT.md

Source files (read before modifying):
@src/transcriber.py
@src/config.py
@src/server.py
@requirements.txt
@tests/test_config.py
@tests/test_whisper_transcriber.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write cleanup assertions, then delete dead files and remove stale code</name>
  <files>
    tests/test_cleanup.py,
    src/coreml_transcriber.py,
    src/transcriber.py,
    src/config.py,
    src/server.py,
    tests/test_stt.py,
    tests/test_config.py,
    tests/test_whisper_transcriber.py,
    models/coreml/
  </files>
  <behavior>
    - test_no_coreml_transcriber_file: src/coreml_transcriber.py does not exist on disk
    - test_no_coreml_models_dir: models/coreml/ directory does not exist
    - test_no_test_stt_file: tests/test_stt.py does not exist on disk
    - test_no_coreml_imports: grep of all .py files under src/ returns zero matches for "coreml_transcriber"
    - test_no_default_engine_in_config: config module has no DEFAULT_ENGINE attribute
    - test_no_engine_name_in_server: src/server.py does not contain "_engine_name"
    - test_no_backward_compat_fields: TranscriptionUpdate has no draft_text, finalized_sentences, or draft_sentences fields
    - test_no_parakeet_in_requirements: requirements.txt does not contain "parakeet-mlx"
    - test_no_coremltools_in_requirements: requirements.txt does not contain "coremltools"
    - test_no_scipy_in_requirements: requirements.txt does not contain "scipy"
    - test_existing_tests_pass: pytest runs cleanly on the full test suite
  </behavior>
  <action>
    **RED phase -- write tests/test_cleanup.py first:**

    Create `tests/test_cleanup.py` with all the assertions listed in behavior. These tests assert the ABSENCE of dead code. They will all fail initially (the dead code still exists). Run `pytest tests/test_cleanup.py` to confirm they fail.

    **GREEN phase -- make all tests pass:**

    Execute these changes in this order to avoid broken intermediate states:

    1. **Delete dead files** (per D-01, D-03, D-04):
       - `git rm src/coreml_transcriber.py`
       - `git rm -r models/coreml/`
       - `git rm tests/test_stt.py`

    2. **Clean TranscriptionUpdate backward-compat fields** (per D-02 intent):
       `src/transcriber.py` was rewritten in Phase 4 as the Whisper engine. Do NOT delete this file. Instead:
       - Remove lines 49-52 (the `draft_text`, `finalized_sentences`, `draft_sentences` fields)
       - Remove the backward-compat docstring lines 40-41 ("Backward-compat fields kept for coreml_transcriber.py (removed in Phase 6): draft_text, finalized_sentences, draft_sentences")
       - Keep all other fields and the entire WhisperTranscriber class intact

    3. **Remove DEFAULT_ENGINE from config.py** (per D-08, D-16):
       - Delete line 39: `DEFAULT_ENGINE: str = "coreml"`
       - The IPC section comment and remaining constants (ENERGY_EMIT_INTERVAL_S, MODEL_LOAD_TIMEOUT_S) stay

    4. **Clean server.py engine remnants** (per D-05, D-07):
       - Remove the `_engine_name` global variable (line 90): `_engine_name: str = "whisper"`
       - In `_do_start()`: remove `engine = data.get("engine", "whisper")` (line 159), remove `_engine_name = engine` (line 163), remove `_engine_name` from the global statement (line 157), and remove `engine=%s` from the log.info format string (line 165) -- change to just `log.info("Starting: device=%s", device)`
       - No other changes to server.py

    5. **Update test_config.py** (per D-14):
       - Delete the `test_default_engine` function (lines 131-133)

    6. **Update test_whisper_transcriber.py**:
       - Remove backward-compat field assertions from `test_transcription_update_fields` (lines 83-86 checking draft_text, finalized_sentences, draft_sentences; and lines 94-96 checking their defaults)
       - Remove the entire `test_transcription_update_backward_compat` function (lines 99-109)

    **Run full test suite:** `pytest tests/ -x` -- all tests must pass including the new cleanup assertions.

    NOTE on D-09, D-10 (SwiftUI engine picker): Already clean -- Phase 4 removed engine @AppStorage and engine picker from AppSettings.swift and SettingsView.swift. No action needed.
  </action>
  <verify>
    <automated>cd /Users/yashdesai/Codebase/Fun/Esper && pytest tests/ -x -q</automated>
  </verify>
  <done>
    - src/coreml_transcriber.py deleted, models/coreml/ deleted, tests/test_stt.py deleted
    - TranscriptionUpdate has exactly 5 fields: text, finalized_text, sentences, no_speech_prob, duration_s
    - config.py has no DEFAULT_ENGINE
    - server.py has no _engine_name or engine selection logic
    - test_config.py has no test_default_engine
    - test_whisper_transcriber.py has no backward-compat assertions
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead dependencies and verify clean install</name>
  <files>requirements.txt</files>
  <action>
    **Remove dead dependencies from requirements.txt** (per D-11, D-12, D-13):

    Edit `requirements.txt` to remove these three lines:
    - `parakeet-mlx` (line 1)
    - `scipy` (line 4) -- was a transitive dependency of Parakeet
    - `coremltools` (line 5)

    The remaining dependencies should be:
    ```
    sounddevice
    numpy
    soundfile
    httpx
    python-dotenv
    silero-vad
    torch
    torchaudio
    mlx-whisper==0.4.3
    ```

    **Verify clean install:** Run `pip install -r requirements.txt` in the existing venv to confirm no missing or conflicting dependencies. The install should complete without errors.

    **Verify import chain:** Run `python -c "from src.transcriber import TranscriptionUpdate, WhisperTranscriber; print('OK')"` to confirm the module still loads.

    **Run cleanup tests again:** `pytest tests/test_cleanup.py -x -q` to confirm requirements assertions pass.
  </action>
  <verify>
    <automated>cd /Users/yashdesai/Codebase/Fun/Esper && pip install -r requirements.txt --quiet 2>&1 | tail -3 && python -c "from src.transcriber import TranscriptionUpdate, WhisperTranscriber; print('import OK')" && pytest tests/test_cleanup.py -x -q</automated>
  </verify>
  <done>
    - requirements.txt contains exactly 9 dependencies (no parakeet-mlx, coremltools, scipy)
    - pip install succeeds with no errors
    - Python import chain (transcriber -> config -> whisper_worker) works
    - All cleanup tests pass
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification:

```bash
# 1. No dead files exist
test ! -f src/coreml_transcriber.py && test ! -d models/coreml/ && test ! -f tests/test_stt.py && echo "Dead files removed"

# 2. No stale imports anywhere in src/
grep -r "coreml_transcriber\|DEFAULT_ENGINE\|_engine_name" src/ && echo "STALE REFS FOUND" || echo "No stale references"

# 3. Full test suite
pytest tests/ -x -q

# 4. Clean import
python -c "from src.transcriber import TranscriptionUpdate, WhisperTranscriber; from src import config; assert not hasattr(config, 'DEFAULT_ENGINE'); print('All clean')"
```
</verification>

<success_criteria>
1. `src/coreml_transcriber.py` and `tests/test_stt.py` are deleted -- `ls` confirms absence
2. `models/coreml/` directory is absent from the working tree
3. `requirements.txt` has no `parakeet-mlx`, `coremltools`, or `scipy` entries
4. `pip install -r requirements.txt` succeeds without error
5. `grep -r "coreml_transcriber\|DEFAULT_ENGINE" src/` returns zero matches
6. `pytest tests/ -x` passes all tests (including new cleanup assertions)
7. TranscriptionUpdate dataclass has exactly 5 fields (no backward-compat fields)
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup/06-01-SUMMARY.md`
</output>
