---
phase: 01-config-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/telegram_sender.py
  - src/server.py
  - src/realtime_demo.py
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "telegram_sender.py reads _MAX_RETRIES, _BACKOFF_BASE, _DRAFT_INTERVAL from config, not local module constants"
    - "server.py uses config.MODEL_LOAD_TIMEOUT_S (120.0) instead of local _MODEL_LOAD_TIMEOUT (30)"
    - "server.py uses config.DEFAULT_ENGINE instead of local _engine_name = 'coreml'"
    - "server.py sets config.TELEGRAM_BOT_TOKEN and config.TELEGRAM_CHAT_ID from the start command data dict before constructing TelegramSender"
    - "realtime_demo.py writes Telegram credentials to config.TELEGRAM_BOT_TOKEN and config.TELEGRAM_CHAT_ID after load_dotenv(), not using os.environ directly downstream"
    - "realtime_demo.py imports SAMPLE_RATE from config, not from audio_capture"
    - "server.py imports SAMPLE_RATE from config, not from audio_capture"
  artifacts:
    - path: "src/telegram_sender.py"
      provides: "Telegram sender reading retry/backoff/interval from config"
      min_lines: 100
    - path: "src/server.py"
      provides: "Server using config for timeout, engine default, and Telegram credential mutation"
      min_lines: 300
    - path: "src/realtime_demo.py"
      provides: "CLI demo writing Telegram creds to config before use"
      min_lines: 200
  key_links:
    - from: "src/server.py"
      to: "src/config.py"
      via: "config.TELEGRAM_BOT_TOKEN mutation in _do_start"
      pattern: "config\\.TELEGRAM_BOT_TOKEN ="
    - from: "src/server.py"
      to: "src/config.py"
      via: "config.MODEL_LOAD_TIMEOUT_S in _load_model_with_timeout"
      pattern: "config\\.MODEL_LOAD_TIMEOUT_S"
    - from: "src/realtime_demo.py"
      to: "src/config.py"
      via: "config.TELEGRAM_BOT_TOKEN mutation in main()"
      pattern: "config\\.TELEGRAM_BOT_TOKEN ="
    - from: "src/telegram_sender.py"
      to: "src/config.py"
      via: "config.TELEGRAM_MAX_RETRIES in _send_message"
      pattern: "config\\.TELEGRAM_MAX_RETRIES"
---

<objective>
Migrate the two entry points (server.py, realtime_demo.py) and telegram_sender.py to use config.py. Entry points mutate config at startup; telegram_sender reads retry/backoff constants from config.

Purpose: Complete the config consolidation so that no module defines its own tunable constants and Telegram credentials flow through config.py at runtime.
Output: All src/ modules import tunables from config.py. The .env file is still read by realtime_demo.py (CLI convenience) but values are written to config fields, not used directly via os.environ.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-consolidation/01-RESEARCH.md
@.planning/phases/01-config-consolidation/01-01-SUMMARY.md

<interfaces>
<!-- From src/config.py (created in Plan 01) — constants these files will read/mutate -->

```python
# Telegram (set at runtime via start command / CLI args)
TELEGRAM_BOT_TOKEN: str = ""
TELEGRAM_CHAT_ID: str = ""
TELEGRAM_STREAM: bool = True
TELEGRAM_MAX_RETRIES: int = 3
TELEGRAM_BACKOFF_BASE: float = 1.0
TELEGRAM_DRAFT_INTERVAL: float = 0.5

# IPC
ENERGY_EMIT_INTERVAL_S: float = 0.1
MODEL_LOAD_TIMEOUT_S: float = 120.0
DEFAULT_ENGINE: str = "coreml"

# Audio (imported by server.py and realtime_demo.py indirectly)
SAMPLE_RATE: int = 16000
```

<!-- Current local constants to remove -->

From src/telegram_sender.py (lines 23-25):
```python
_MAX_RETRIES = 3
_BACKOFF_BASE = 1.0
_DRAFT_INTERVAL = 0.5
```

From src/server.py (lines 70-72):
```python
_engine_name: str = "coreml"
_MODEL_LOAD_TIMEOUT = 30
```

From src/server.py (line 59):
```python
from .audio_capture import AudioCapture, SAMPLE_RATE
```

From src/realtime_demo.py (line 28):
```python
from .audio_capture import AudioCapture, SAMPLE_RATE, list_devices
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate telegram_sender.py to read constants from config</name>
  <files>src/telegram_sender.py</files>
  <read_first>
    src/config.py
    src/telegram_sender.py
  </read_first>
  <action>
**src/telegram_sender.py changes:**

1. Add import at top (after existing imports, before `from .transcriber`): `from . import config`

2. Remove these three module-level constants (lines 23-25):
```python
_MAX_RETRIES = 3
_BACKOFF_BASE = 1.0
_DRAFT_INTERVAL = 0.5
```

3. Replace usages of the removed constants with config reads:
   - In `_send_draft()`: `if now - self._last_draft_t < _DRAFT_INTERVAL:` → `if now - self._last_draft_t < config.TELEGRAM_DRAFT_INTERVAL:`
   - In `_send_message()`: `for attempt in range(_MAX_RETRIES):` → `for attempt in range(config.TELEGRAM_MAX_RETRIES):`
   - In `_send_message()`: `time.sleep(_BACKOFF_BASE * (2**attempt))` → `time.sleep(config.TELEGRAM_BACKOFF_BASE * (2**attempt))`
   - In `_send_message()`: `log.error("Failed to send after %d attempts: %s", _MAX_RETRIES, text[:60])` → `log.error("Failed to send after %d attempts: %s", config.TELEGRAM_MAX_RETRIES, text[:60])`

Do NOT change the `TelegramSender.__init__` signature — it still takes `(bot_token, chat_id, *, stream=True)`. That signature cleanup is deferred to Phase 5 per RESEARCH.md.
  </action>
  <verify>
    <automated>cd /Users/yashdesai/Codebase/Fun/Esper && python -c "
from src.telegram_sender import TelegramSender
import src.telegram_sender as ts
# Verify no local constants remain
assert not hasattr(ts, '_MAX_RETRIES'), '_MAX_RETRIES still exists as module attr'
assert not hasattr(ts, '_BACKOFF_BASE'), '_BACKOFF_BASE still exists as module attr'
assert not hasattr(ts, '_DRAFT_INTERVAL'), '_DRAFT_INTERVAL still exists as module attr'
print('telegram_sender OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - src/telegram_sender.py contains `from . import config`
    - src/telegram_sender.py does NOT contain `_MAX_RETRIES = 3`
    - src/telegram_sender.py does NOT contain `_BACKOFF_BASE = 1.0`
    - src/telegram_sender.py does NOT contain `_DRAFT_INTERVAL = 0.5`
    - src/telegram_sender.py contains `config.TELEGRAM_MAX_RETRIES`
    - src/telegram_sender.py contains `config.TELEGRAM_BACKOFF_BASE`
    - src/telegram_sender.py contains `config.TELEGRAM_DRAFT_INTERVAL`
    - TelegramSender.__init__ signature is unchanged: `(self, bot_token: str, chat_id: str, *, stream: bool = True)`
    - `python -c "from src.telegram_sender import TelegramSender"` exits 0
  </acceptance_criteria>
  <done>telegram_sender.py reads all retry/backoff/interval constants from config.py. No local tunable constants remain. Import succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate server.py and realtime_demo.py to use config for constants and credential mutation</name>
  <files>src/server.py, src/realtime_demo.py</files>
  <read_first>
    src/config.py
    src/server.py
    src/realtime_demo.py
  </read_first>
  <action>
**src/server.py changes:**

1. Add import (after the `_proto_out` setup, near the other imports around line 56): `from . import config`

2. Change the audio_capture import on line 59:
   - Before: `from .audio_capture import AudioCapture, SAMPLE_RATE`
   - After: `from .audio_capture import AudioCapture`
   (SAMPLE_RATE is no longer exported from audio_capture; if server.py still needs it directly, use `config.SAMPLE_RATE`. Searching the file: SAMPLE_RATE is imported but never used directly in server.py — it was imported as a pass-through. Remove it.)

3. Remove module-level state constants (lines 70-72):
   - Remove: `_engine_name: str = "coreml"`
   - Remove: `_MODEL_LOAD_TIMEOUT = 30`

4. Replace `_engine_name` default initialization. Keep `_engine_name` as a module-level variable but initialize from config:
   - Replace: `_engine_name: str = "coreml"`
   - With: `_engine_name: str = config.DEFAULT_ENGINE`

5. In `_load_model_with_timeout()`:
   - Replace: `thread.join(timeout=_MODEL_LOAD_TIMEOUT)` → `thread.join(timeout=config.MODEL_LOAD_TIMEOUT_S)`
   - Replace: `raise TimeoutError(f"Model loading timed out after {_MODEL_LOAD_TIMEOUT}s")` → `raise TimeoutError(f"Model loading timed out after {config.MODEL_LOAD_TIMEOUT_S}s")`

6. In `_do_start(data)`:
   - Replace: `engine = data.get("engine", "coreml")` → `engine = data.get("engine", config.DEFAULT_ENGINE)`
   - Add config mutation for Telegram credentials BEFORE constructing TelegramSender. After `if telegram_cfg:` and before `_telegram_sender = TelegramSender(...)`, add:
     ```python
     config.TELEGRAM_BOT_TOKEN = bot_token
     config.TELEGRAM_CHAT_ID = chat_id
     config.TELEGRAM_STREAM = stream
     ```
     (Keep the existing `TelegramSender(bot_token, chat_id, stream=stream)` constructor call — it still takes explicit args.)

7. In `_emit_energy()`:
   - Replace: `time.sleep(0.1)` → `time.sleep(config.ENERGY_EMIT_INTERVAL_S)`

**src/realtime_demo.py changes:**

1. Add import at top (after existing imports, before `from .audio_capture`): `from . import config`

2. Change the audio_capture import on line 28:
   - Before: `from .audio_capture import AudioCapture, SAMPLE_RATE, list_devices`
   - After: `from .audio_capture import AudioCapture, list_devices`

3. In the Telegram setup block inside `main()` (around lines 103-112):
   - After `load_dotenv()` and reading `bot_token`/`chat_id` from `os.environ`, add:
     ```python
     config.TELEGRAM_BOT_TOKEN = bot_token
     config.TELEGRAM_CHAT_ID = chat_id
     ```
   - Keep the existing `TelegramSender(bot_token, chat_id)` constructor call unchanged.

4. Replace `SAMPLE_RATE` references with `config.SAMPLE_RATE`:
   - Line 224: `sf.write(str(record_path), audio, SAMPLE_RATE)` → `sf.write(str(record_path), audio, config.SAMPLE_RATE)`
   - Line 225: `duration = len(audio) / SAMPLE_RATE` → `duration = len(audio) / config.SAMPLE_RATE`

Do NOT touch realtime_demo.py's `print()` calls — they are intentional CLI output per RESEARCH.md Open Question 2.
  </action>
  <verify>
    <automated>cd /Users/yashdesai/Codebase/Fun/Esper && python -c "
import src.server as srv
import src.config as config
# Verify _MODEL_LOAD_TIMEOUT removed
assert not hasattr(srv, '_MODEL_LOAD_TIMEOUT'), '_MODEL_LOAD_TIMEOUT still exists'
# Verify config has correct timeout
assert config.MODEL_LOAD_TIMEOUT_S == 120.0, f'MODEL_LOAD_TIMEOUT_S={config.MODEL_LOAD_TIMEOUT_S}'
print('server.py OK')
" && python -c "
# Verify realtime_demo doesn't import SAMPLE_RATE from audio_capture
import ast
tree = ast.parse(open('src/realtime_demo.py').read())
for node in ast.walk(tree):
    if isinstance(node, ast.ImportFrom) and node.module and 'audio_capture' in node.module:
        names = [a.name for a in node.names]
        assert 'SAMPLE_RATE' not in names, 'realtime_demo.py still imports SAMPLE_RATE from audio_capture'
print('realtime_demo.py OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - src/server.py contains `from . import config`
    - src/server.py does NOT contain `_MODEL_LOAD_TIMEOUT = 30`
    - src/server.py contains `config.MODEL_LOAD_TIMEOUT_S`
    - src/server.py contains `config.DEFAULT_ENGINE`
    - src/server.py contains `config.TELEGRAM_BOT_TOKEN = bot_token`
    - src/server.py contains `config.TELEGRAM_CHAT_ID = chat_id`
    - src/server.py contains `config.ENERGY_EMIT_INTERVAL_S`
    - src/server.py does NOT contain `from .audio_capture import AudioCapture, SAMPLE_RATE` (SAMPLE_RATE removed from import)
    - src/realtime_demo.py contains `from . import config`
    - src/realtime_demo.py does NOT contain `from .audio_capture import AudioCapture, SAMPLE_RATE, list_devices` (SAMPLE_RATE removed)
    - src/realtime_demo.py contains `config.TELEGRAM_BOT_TOKEN =`
    - src/realtime_demo.py contains `config.SAMPLE_RATE`
    - `python -c "import src.server"` exits 0
  </acceptance_criteria>
  <done>server.py uses config for timeout (120s), engine default, energy interval, and mutates config.TELEGRAM_* before constructing TelegramSender. realtime_demo.py writes Telegram creds to config after load_dotenv(). Both files import SAMPLE_RATE from config, not audio_capture. All imports succeed.</done>
</task>

</tasks>

<verification>
1. `python -c "from src import config; from src.audio_capture import AudioCapture; from src.transcriber import TranscriptionUpdate; from src.telegram_sender import TelegramSender; print('all imports OK')"` — full import chain works
2. `python -c "from src.config import SAMPLE_RATE, VAD_FRAME_SIZE, WHISPER_MODEL_REPO, TELEGRAM_DRAFT_INTERVAL, ENERGY_EMIT_INTERVAL_S; print('all namespaces OK')"` — all stubs accessible
3. `python -c "import ast; tree=ast.parse(open('src/audio_capture.py').read()); assert not any(isinstance(n,ast.Call) and getattr(n.func,'id','')==='print' for n in ast.walk(tree)); print('no print() in audio_capture')"` — IPC safety confirmed
4. `python -c "import src.config as c; assert c.MODEL_LOAD_TIMEOUT_S == 120.0; print('timeout updated')"` — CLEAN-03 satisfied
5. `python -c "import src.server"` — server imports cleanly (this exercises the stdout redirect + config import chain)
</verification>

<success_criteria>
- All 7 src/ files either define no local tunables (leaf modules) or mutate config at startup (entry points)
- Telegram credentials flow through config.py — server.py sets them from start command, realtime_demo.py sets them from .env
- MODEL_LOAD_TIMEOUT_S is 120.0 (up from 30)
- No module outside config.py defines SAMPLE_RATE, CHANNELS, CHUNK_DURATION, QUEUE_MAXSIZE, _MAX_RETRIES, _BACKOFF_BASE, _DRAFT_INTERVAL, or _MODEL_LOAD_TIMEOUT
- Full import chain `python -c "import src.server"` succeeds (proves no circular imports, no missing names)
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-consolidation/01-02-SUMMARY.md`
</output>
