---
phase: 01-config-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/audio_capture.py
  - src/transcriber.py
  - src/coreml_transcriber.py
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "All tunable constants live in src/config.py — no module defines its own copy"
    - "Importing src.config succeeds and exposes SAMPLE_RATE, CHANNELS, CHUNK_DURATION, CHUNK_SAMPLES, QUEUE_MAXSIZE"
    - "VAD, Whisper, Telegram, and IPC constant namespaces are present in config.py with documented defaults"
    - "audio_capture.py contains zero print() calls — the IPC corruption landmine is removed"
    - "audio_capture.py, transcriber.py, coreml_transcriber.py all import SAMPLE_RATE from config, not from audio_capture"
  artifacts:
    - path: "src/config.py"
      provides: "Single source of truth for all Esper tunables"
      contains: "SAMPLE_RATE"
    - path: "src/audio_capture.py"
      provides: "Audio capture importing from config"
      min_lines: 80
  key_links:
    - from: "src/audio_capture.py"
      to: "src/config.py"
      via: "from . import config"
      pattern: "from . import config"
    - from: "src/transcriber.py"
      to: "src/config.py"
      via: "from . import config"
      pattern: "from . import config"
    - from: "src/coreml_transcriber.py"
      to: "src/config.py"
      via: "from . import config"
      pattern: "from . import config"
---

<objective>
Create `src/config.py` as the single source of truth for all Esper tunables, then migrate audio_capture.py, transcriber.py, and coreml_transcriber.py to import from it. Replace the dangerous `print()` call in audio_capture.py with `log.info()`.

Purpose: Eliminate scattered constants across modules so every component reads from one place. This is the foundation for all subsequent phases.
Output: `src/config.py` with all namespaces; three leaf modules updated to import from config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-consolidation/01-RESEARCH.md

<interfaces>
<!-- Current constant locations that must be moved to config.py -->

From src/audio_capture.py (lines 13-17):
```python
SAMPLE_RATE = 16000
CHANNELS = 1
CHUNK_DURATION = 0.1  # seconds per callback chunk
CHUNK_SAMPLES = int(SAMPLE_RATE * CHUNK_DURATION)  # 1600 samples
QUEUE_MAXSIZE = 300  # ~30 seconds of buffered audio
```

From src/transcriber.py (line 13):
```python
from .audio_capture import SAMPLE_RATE
```

From src/coreml_transcriber.py (line 23):
```python
from .audio_capture import SAMPLE_RATE
```

From src/audio_capture.py (line 75 — IPC corruption landmine):
```python
print(f"  Device: [{dev_idx}] {dev_name}")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/config.py with all namespaced constants</name>
  <files>src/config.py</files>
  <read_first>
    src/audio_capture.py
    src/telegram_sender.py
    src/server.py
    .planning/phases/01-config-consolidation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `from src import config` succeeds without error
    - Test 2: `config.SAMPLE_RATE == 16000`
    - Test 3: `config.CHANNELS == 1`
    - Test 4: `config.CHUNK_DURATION == 0.1`
    - Test 5: `config.CHUNK_SAMPLES == 1600`
    - Test 6: `config.QUEUE_MAXSIZE == 300`
    - Test 7: `config.VAD_FRAME_SIZE == 512` (Phase 3 stub)
    - Test 8: `config.WHISPER_MODEL_REPO == "mlx-community/whisper-large-v3-turbo"` (Phase 4 stub)
    - Test 9: `config.TELEGRAM_BOT_TOKEN == ""` (empty default, set at runtime)
    - Test 10: `config.TELEGRAM_MAX_RETRIES == 3`
    - Test 11: `config.TELEGRAM_BACKOFF_BASE == 1.0`
    - Test 12: `config.TELEGRAM_DRAFT_INTERVAL == 0.5`
    - Test 13: `config.ENERGY_EMIT_INTERVAL_S == 0.1`
    - Test 14: `config.MODEL_LOAD_TIMEOUT_S == 120.0` (updated from 30 per CLEAN-03)
    - Test 15: `config.DEFAULT_ENGINE == "coreml"`
  </behavior>
  <action>
Create `src/config.py` with the following exact content structure. The file must import nothing from `src/` (zero internal imports — prevents circular imports per Pitfall B in RESEARCH.md).

Five namespaced sections with these exact constants and values:

**Audio section:**
- `SAMPLE_RATE: int = 16000`
- `CHANNELS: int = 1`
- `CHUNK_DURATION: float = 0.1` (seconds per sounddevice callback chunk)
- `CHUNK_SAMPLES: int = int(SAMPLE_RATE * CHUNK_DURATION)` (derived: 1600 samples)
- `QUEUE_MAXSIZE: int = 300` (~30s of buffered audio)

**VAD section (Phase 3 stubs):**
- `VAD_FRAME_SIZE: int = 512` (samples at 16kHz = 32ms, hard Silero requirement)
- `VAD_SPEECH_THRESHOLD: float = 0.5`
- `VAD_SILENCE_THRESHOLD_MS: int = 500`
- `VAD_MIN_SPEECH_DURATION_MS: int = 500`
- `VAD_MIN_ENERGY: float = 0.01`

**Whisper section (Phase 4 stubs):**
- `WHISPER_MODEL_REPO: str = "mlx-community/whisper-large-v3-turbo"`
- `WHISPER_LANGUAGE: str = "en"`
- `WHISPER_MAX_GENERATIONS_BEFORE_RESTART: int = 50`
- `WHISPER_SUBPROCESS_TIMEOUT_S: float = 15.0`

**Telegram section:**
- `TELEGRAM_BOT_TOKEN: str = ""` (set at runtime via start command / CLI args)
- `TELEGRAM_CHAT_ID: str = ""`
- `TELEGRAM_STREAM: bool = True`
- `TELEGRAM_MAX_RETRIES: int = 3`
- `TELEGRAM_BACKOFF_BASE: float = 1.0`
- `TELEGRAM_DRAFT_INTERVAL: float = 0.5`

**IPC section:**
- `ENERGY_EMIT_INTERVAL_S: float = 0.1` (~10 Hz energy events)
- `MODEL_LOAD_TIMEOUT_S: float = 120.0` (includes MLX Metal shader compile on cold start — updated from 30 per CLEAN-03)
- `DEFAULT_ENGINE: str = "coreml"`

Module docstring: "Single source of truth for all Esper tunables. Every component imports from here. Mutation happens only at startup (in server._do_start or realtime_demo.main) before any component is constructed."

Write the test file `tests/test_config.py` FIRST (RED), then create `src/config.py` (GREEN).
  </action>
  <verify>
    <automated>cd /Users/yashdesai/Codebase/Fun/Esper && python -c "from src import config; assert config.SAMPLE_RATE == 16000; assert config.CHUNK_SAMPLES == 1600; assert config.VAD_FRAME_SIZE == 512; assert config.WHISPER_MODEL_REPO == 'mlx-community/whisper-large-v3-turbo'; assert config.TELEGRAM_DRAFT_INTERVAL == 0.5; assert config.ENERGY_EMIT_INTERVAL_S == 0.1; assert config.MODEL_LOAD_TIMEOUT_S == 120.0; assert config.DEFAULT_ENGINE == 'coreml'; print('config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/config.py exists and contains `SAMPLE_RATE: int = 16000`
    - src/config.py contains `VAD_FRAME_SIZE: int = 512`
    - src/config.py contains `WHISPER_MODEL_REPO: str = "mlx-community/whisper-large-v3-turbo"`
    - src/config.py contains `TELEGRAM_BOT_TOKEN: str = ""`
    - src/config.py contains `TELEGRAM_MAX_RETRIES: int = 3`
    - src/config.py contains `TELEGRAM_BACKOFF_BASE: float = 1.0`
    - src/config.py contains `TELEGRAM_DRAFT_INTERVAL: float = 0.5`
    - src/config.py contains `ENERGY_EMIT_INTERVAL_S: float = 0.1`
    - src/config.py contains `MODEL_LOAD_TIMEOUT_S: float = 120.0`
    - src/config.py contains `DEFAULT_ENGINE: str = "coreml"`
    - src/config.py does NOT contain `from .` or `from src.` (zero internal imports)
    - `python -c "from src import config"` exits 0
    - tests/test_config.py exists and passes
  </acceptance_criteria>
  <done>src/config.py exists with all 5 namespaced sections, all constants have correct default values, module imports cleanly with no internal dependencies, test file passes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate audio_capture.py, transcriber.py, coreml_transcriber.py to import from config</name>
  <files>src/audio_capture.py, src/transcriber.py, src/coreml_transcriber.py</files>
  <read_first>
    src/config.py
    src/audio_capture.py
    src/transcriber.py
    src/coreml_transcriber.py
  </read_first>
  <action>
**src/audio_capture.py changes:**

1. Remove these five lines (lines 13-17):
```python
SAMPLE_RATE = 16000
CHANNELS = 1
CHUNK_DURATION = 0.1
CHUNK_SAMPLES = int(SAMPLE_RATE * CHUNK_DURATION)
QUEUE_MAXSIZE = 300
```

2. Add at the top (after existing imports): `from . import config`

3. Replace every bare reference to the removed constants with `config.` prefix:
   - `QUEUE_MAXSIZE` on line 64 → `config.QUEUE_MAXSIZE`
   - `SAMPLE_RATE` on line 78 → `config.SAMPLE_RATE`
   - `CHANNELS` on line 79 → `config.CHANNELS`
   - `CHUNK_SAMPLES` on line 81 → `config.CHUNK_SAMPLES`

4. Replace the `print()` call on line 75:
   - Before: `print(f"  Device: [{dev_idx}] {dev_name}")`
   - After: `log.info("Audio capture starting on device [%d] %s", dev_idx, dev_name)`

5. Convert the two `print()` calls in `list_devices()` (lines 22, 26) to `log.info()`:
   - Before: `print("\nAvailable audio input devices:")`
   - After: `log.info("Available audio input devices:")`
   - Before: `print(f"  [{i}] {dev['name']}{default}")`
   - After: `log.info("  [%d] %s%s", i, dev['name'], default)`

**src/transcriber.py changes:**

1. Replace line 13: `from .audio_capture import SAMPLE_RATE`
   - With: `from . import config`
2. Replace `SAMPLE_RATE` usage on line 102: `silence = mx.zeros((int(SAMPLE_RATE * 0.5),))`
   - With: `silence = mx.zeros((int(config.SAMPLE_RATE * 0.5),))`

**src/coreml_transcriber.py changes:**

1. Replace line 23: `from .audio_capture import SAMPLE_RATE`
   - With: `from . import config`
2. Replace every `SAMPLE_RATE` usage with `config.SAMPLE_RATE`:
   - Line 34: `OVERLAP_SAMPLES = SAMPLE_RATE` → `OVERLAP_SAMPLES = config.SAMPLE_RATE`
   - Line 221: `SAMPLE_RATE * min(...)` → `config.SAMPLE_RATE * min(...)`
   - Line 340: `int(self.buffer_seconds * SAMPLE_RATE)` → `int(self.buffer_seconds * config.SAMPLE_RATE)`
   - Line 345: `np.zeros(SAMPLE_RATE, ...)` → `np.zeros(config.SAMPLE_RATE, ...)`
   - Line 372: `len(buffer) > SAMPLE_RATE * 0.3` → `len(buffer) > config.SAMPLE_RATE * 0.3`

Do NOT change `audio_capture.py`'s `list_devices()` function signature, `find_real_mic()`, `auto_select_device()`, `AudioCapture` class API, or `get_chunk`/`stop`/`start`/`energy` methods — only constants and print calls.
  </action>
  <verify>
    <automated>cd /Users/yashdesai/Codebase/Fun/Esper && python -c "
from src import config
from src.audio_capture import AudioCapture, list_devices, find_real_mic, auto_select_device
from src.transcriber import TranscriptionUpdate, StreamingTranscriber
assert not hasattr(__import__('src.audio_capture', fromlist=['SAMPLE_RATE']), 'SAMPLE_RATE') or True
print('All imports OK')
" && python -c "
import ast
tree = ast.parse(open('src/audio_capture.py').read())
prints = [n for n in ast.walk(tree) if isinstance(n, ast.Call) and getattr(n.func, 'id', '') == 'print']
assert len(prints) == 0, f'Found {len(prints)} print() calls in audio_capture.py'
print('No print() calls in audio_capture.py')
"</automated>
  </verify>
  <acceptance_criteria>
    - src/audio_capture.py contains `from . import config`
    - src/audio_capture.py does NOT contain `SAMPLE_RATE = 16000`
    - src/audio_capture.py does NOT contain `CHANNELS = 1` as a module-level constant
    - src/audio_capture.py does NOT contain `CHUNK_DURATION = 0.1` as a module-level constant
    - src/audio_capture.py does NOT contain `QUEUE_MAXSIZE = 300` as a module-level constant
    - src/audio_capture.py contains `config.SAMPLE_RATE`
    - src/audio_capture.py contains `config.CHANNELS`
    - src/audio_capture.py contains `config.CHUNK_SAMPLES`
    - src/audio_capture.py contains `config.QUEUE_MAXSIZE`
    - src/audio_capture.py contains `log.info` (replacing print)
    - src/audio_capture.py does NOT contain any `print(` calls (zero occurrences)
    - src/transcriber.py contains `from . import config`
    - src/transcriber.py does NOT contain `from .audio_capture import SAMPLE_RATE`
    - src/transcriber.py contains `config.SAMPLE_RATE`
    - src/coreml_transcriber.py contains `from . import config`
    - src/coreml_transcriber.py does NOT contain `from .audio_capture import SAMPLE_RATE`
    - src/coreml_transcriber.py contains `config.SAMPLE_RATE`
    - `python -c "from src.audio_capture import AudioCapture"` exits 0
    - `python -c "from src.transcriber import TranscriptionUpdate"` exits 0
  </acceptance_criteria>
  <done>All three files import constants from config.py instead of defining their own. audio_capture.py has zero print() calls. All module imports succeed without error.</done>
</task>

</tasks>

<verification>
1. `python -c "from src import config; assert config.SAMPLE_RATE == 16000; assert config.MODEL_LOAD_TIMEOUT_S == 120.0; print('config OK')"` — config module loads with correct values
2. `python -c "from src.audio_capture import AudioCapture; print('audio OK')"` — audio_capture imports config correctly
3. `python -c "from src.transcriber import TranscriptionUpdate, StreamingTranscriber; print('transcriber OK')"` — transcriber imports config correctly
4. `python -c "from src.config import VAD_FRAME_SIZE, WHISPER_MODEL_REPO, TELEGRAM_DRAFT_INTERVAL, ENERGY_EMIT_INTERVAL_S; print('all namespaces OK')"` — all future-phase stubs present
5. `python -c "import ast; tree=ast.parse(open('src/audio_capture.py').read()); prints=[n for n in ast.walk(tree) if isinstance(n, ast.Call) and getattr(n.func,'id','')==='print']; assert len(prints)==0; print('no print() calls')"` — IPC corruption risk eliminated
</verification>

<success_criteria>
- src/config.py exists with Audio, VAD, Whisper, Telegram, and IPC sections
- audio_capture.py, transcriber.py, coreml_transcriber.py all import from config
- audio_capture.py has zero print() calls
- All `python -c "from src.X import Y"` commands exit 0 for every public name
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-consolidation/01-01-SUMMARY.md`
</output>
